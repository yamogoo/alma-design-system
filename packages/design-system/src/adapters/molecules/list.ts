import {
  groupModes,
  groupSizes,
  groupTones,
  groupVariants,
} from "@/adapters/atoms/group";

export const listVariants = groupVariants;
export const listSizes = groupSizes;
export const listModes = groupModes;
export const listTones = groupTones;
